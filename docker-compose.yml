services:
  postgres:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=sujan6861
    ports:
      - 5432:5432
    volumes:
      - /var/lib/postresql/data
  mongodb:
    image: mongo
    ports:
      - 27017:27017
    volumes:
      - /data/db
   